# 行情看板

一个使用 Tkinter + Matplotlib 编写的桌面行情看板，支持 A 股和加密货币行情查看。

## 运行

```powershell
python main.py
```

## 安装依赖

```powershell
python -m pip install -r requirements.txt
```

## 打包

```powershell
python build_installer.py
```

打包后生成的文件位于 `dist/`，该目录不提交到 GitHub。
